<!-- navigation.nunjucks -->
<ul class="nav navbar-nav">
  {% for item in menu %}
  <li {% if item.active %}class="active"{% endif %}>
    <a href="{{item.url}}">{{item.label}}</a>
  </li>
  {% endfor %}
</ul>
